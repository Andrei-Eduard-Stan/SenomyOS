(defwidget wifi-button [] 
      (button
      :class "icon-button mainbar-wifi-button"
      :onclick "if [[ \"$(eww get panel)\" = action && \"$(eww get ac_page)\" = network ]]; then eww update panel=none; else eww update panel=action; eww update ac_page=network; fi"
       ""))