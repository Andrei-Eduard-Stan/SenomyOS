(defwindow main-bar
  :geometry (geometry :x "0" :y "1040" :width "100%" :height "40px")
  :stacking "fg"
  :monitor 0
  :exclusive true
  :anchor "top center"
  :visible true

  (centerbox :class "bar" :orientation "h"
    ;; left
    (box
      :class "mainbar-left"
      :halign "start"
      :space-evenly false
      (workspace-indicator))

    ;; center
    (system-summary)

    ;; right
    (box
      :class "mainbar-right"
      :spacing 10
      :halign "end"
      :space-evenly false
      (clock-button)
      (volume-button)
      (wifi-button)
      (battery-button))))
         